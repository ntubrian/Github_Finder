const zh = ({ toggle }) => {
  //   useEffect(() => {
  //     console.log({ router });
  //   });

  return (
    <div
      className={`text-white ${toggle ? "font-extrabold text-[#4b81ad]" : ""}`}
    >
      zh
    </div>
  );
};

export default zh;
